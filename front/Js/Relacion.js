/*document.addEventListener('DOMContentLoaded', () => {
  const relacionElement = document.getElementById('relacion');
  if (relacionElement) {
      relacionElement.addEventListener('click', () => {
          fetch('/consulta')
              .then(response => response.json())
              .then(data => {
                  console.log(data);
              })
              .catch(error => {
                  console.error('Error:', error);
              });
      });
  } else {
      console.error('Elemento con ID "relacion" no encontrado');
  }
});*/
